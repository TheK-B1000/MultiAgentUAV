# Phase 0 - Discovery & Baseline

Date: 2026-04-24

Scope: discovery only. I excluded `.venv`, `.pytest_cache`, and `__pycache__` from source inventory. The repo contains pre-existing local changes:

```text
 M AICTFProject/plot/plot_metrics.py
?? AICTFProject/checkpoints_sb3/
```

Git initially refused normal commands because the repository is marked as dubious ownership for the current Windows user. I used per-command `safe.directory` flags for read-only Git inspection.

## 1. Repo Inventory

### Project Metadata

- `.gitignore` - root ignore rules.
- `.idea/*` - tracked JetBrains project metadata.
- `AICTFProject/.idea/*` - tracked nested JetBrains project metadata.

### Environment And Game Logic

- `AICTFProject/game_field_gpu.py` - primary GPU/torch batched CTF simulator, SB3 `VecEnv`, single Gymnasium adapter, observations, actions, rewards, terminal info.
- `AICTFProject/game_manager.py` - non-GPU game manager plus shared scoring and reward constants imported by the GPU env.
- `AICTFProject/agents.py` - minimal `AgentHandle` used by adapters and policy-facing game APIs.
- `AICTFProject/macro_actions.py` - `MacroAction` enum for high-level actions.
- `AICTFProject/opponent_params.py` - batched scripted/species/snapshot opponent parameter sampler.

### Networks And RL

- `AICTFProject/rl/ctf_cnn_extractor.py` - SB3 feature extractor with per-agent CNN over local grid observations plus vector feature concatenation.
- `AICTFProject/rl/latent_marl.py` - latent-strategy policy, strategy encoder, centralized action-conditioned critic, and PPO subclass with latent regularizers.
- `AICTFProject/rl/latent_vec_env.py` - SB3 `VecEnvWrapper` that samples/attaches latent strategy fields and global state.
- `AICTFProject/rl/global_state.py` - builds a padded global-state feature tensor from the batched core.
- `AICTFProject/rl/train_ppo.py` - main PPO/SB3 training script, masked policy, callbacks, seeding, train modes, smoke helpers.
- `AICTFProject/rl/stress_schedule.py` - phase-to-physics stress override table.
- `AICTFProject/rl/eval_mode.py` - deterministic evaluation mode config and guard helpers.
- `AICTFProject/rl/episode_result.py` - typed parser for `info["episode_result"]`.
- `AICTFProject/rl/__init__.py` - empty package marker.

### Evaluation, Plotting, And Viewer

- `AICTFProject/ctfviewer.py` - Pygame/SB3 viewer and headless evaluation harness.
- `AICTFProject/plot/eval_rollout.py` - reusable evaluation episode runner and aggregate metric helpers.
- `AICTFProject/plot/plot_2v2_winrate.py` - CLI to evaluate/plot 2v2 win-rate comparison checkpoints.
- `AICTFProject/plot/plot_eval_metrics.py` - CLI to evaluate/plot multi-size metrics or plot frozen CSV metrics.
- `AICTFProject/plot/plot_metrics.py` - CLI to plot training metrics CSVs.

### Configs

- `AICTFProject/configs/eval.yaml` - deterministic evaluation seeds, opponent suite, env locks, metrics, and output names.

### Tests

- `AICTFProject/tests/test_latent_strategy_alignment.py` - unittest checks strategy encoder shape, persistence penalty, and decentralized actor behavior.
- `AICTFProject/tests/test_opponent_params.py` - unittest checks OP4 bounds, regime diversity, and deterministic sampling.

### Docs

- `AICTFProject/docs/SETUP_AND_TRAIN.md` - install/training guide for local and Colab runs.

### Data And Checkpoints

- `AICTFProject/csv/*.csv` - tracked evaluation/robustness metrics tables.
- `AICTFProject/rl/checkpoints_sb3/2v2/crash_save_ppo_league_curriculum_2v2.zip` - tracked crash checkpoint.
- `AICTFProject/checkpoints_sb3/2v2/final_marl_latent_fixed_op3_2v2.zip` - untracked local trained checkpoint.
- `AICTFProject/checkpoints_sb3/2v2/marl_latent_fixed_op3_2v2_metrics.csv` - untracked local training metrics.
- `AICTFProject/checkpoints_sb3/2v2/final_codex_phase0_smoke_2v2.zip` - generated by the Phase 0 smoke run.

## 2. Dependency Inventory

No dependency manifest was found in tracked source:

- No `requirements.txt`
- No `pyproject.toml`
- No `environment.yml`
- No `setup.py`
- No `setup.cfg`
- No pytest config file

The project-local `.venv` exists but its launcher is broken:

```text
Unable to create process using '"C:\Users\K-B\AppData\Local\Programs\Python\Python312\python.exe" --version'
```

I used the bundled Codex Python 3.12.13 with `AICTFProject/.venv/Lib/site-packages` on `PYTHONPATH` for baseline commands.

Observed runtime package versions from that site-packages directory:

- Python: 3.12.13 via `C:\Users\K-B\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`
- PyTorch: `torch==2.10.0+cpu`
- Torchvision: `torchvision==0.19.1`
- Torch DirectML: `torch-directml==0.2.5.dev240914`
- Env framework: `gymnasium==1.2.3`; custom SB3 `VecEnv`; no PettingZoo
- RL library: `stable_baselines3==2.7.1`
- Logger tooling: TensorBoard package `tensorboard==2.20.0`; SB3 logger used in `train_ppo.py`; no W&B import found
- Rendering/eval: `pygame==2.6.1`, `matplotlib==3.10.8`, `pandas==2.3.3`
- Test runner: `pytest` is not installed; existing tests are `unittest` compatible

Docs are stale relative to the local runtime: `AICTFProject/docs/SETUP_AND_TRAIN.md` instructs installing `stable-baselines3==2.3.0`, `gymnasium==0.29.1`, and `pygame==2.5.2`.

## 3. Entry Points

### Training

`AICTFProject/rl/train_ppo.py`

CLI:

```text
--mode MODE
--run-tag RUN_TAG
--total-steps TOTAL_STEPS
--checkpoint-dir CHECKPOINT_DIR
--load LOAD
--fixed-opponent FIXED_OPPONENT
--agents {2,4,6,8}
--max-blue-agents MAX_BLUE_AGENTS
--gpu-native-env
--test-kl-zero-lr
--verbose-training
--device DEVICE
--latent-strategy
--no-latent-strategy
--latent-k {4,6}
--latent-lam-h LATENT_LAM_H
--latent-lam-p LATENT_LAM_P
--latent-resample-n LATENT_RESAMPLE_N
```

Hidden direct modes detected before argparse:

- `--verify-4v4`
- `--test-vec-schema`

Training defaults currently enable the latent-strategy path unless `--no-latent-strategy` is passed.

### Viewer / Headless Eval

`AICTFProject/ctfviewer.py`

CLI:

```text
--ppo-model PPO_MODEL
--eval N
--headless
--device DEVICE
--stochastic
```

### Evaluation / Plotting

`AICTFProject/plot/plot_2v2_winrate.py`

CLI:

```text
--league LEAGUE
--paper PAPER
--selfplay SELFPLAY
--episodes EPISODES
--opponent OPPONENT
--seed SEED
--match-eval
--match-eval-op3
--out OUT
--device DEVICE
```

`AICTFProject/plot/plot_eval_metrics.py`

CLI includes checkpoint inputs for 2v2, 3v3, 4v4, 5v5, and 8v8; `--episodes`; `--modes`; `--opponent`/`--opponents`; `--robustness-modes`; `--out`; `--table-out`; `--table-opponent`; `--device`; `--training-csv`; and `--from-csv`/`--metrics-csv`.

`AICTFProject/plot/plot_metrics.py`

CLI:

```text
csv [csv ...]
--out-dir OUT_DIR
--window WINDOW
--no-collisions
```

`AICTFProject/plot/eval_rollout.py` has evaluation helper functions but no CLI `main` block.

## 4. Existing Tests

Command attempted:

```powershell
$env:PYTHONPATH='...\AICTFProject\.venv\Lib\site-packages'
& '...\codex-primary-runtime\dependencies\python\python.exe' -m pytest -q
```

Result:

```text
C:\Users\K-B\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe: No module named pytest
```

Fallback command because the existing tests are `unittest` based:

```powershell
$env:PYTHONPATH='...\AICTFProject\.venv\Lib\site-packages'
& '...\codex-primary-runtime\dependencies\python\python.exe' -m unittest discover -v tests
```

Result:

```text
test_actor_is_decentralized_per_agent_given_shared_z ... ok
test_persistence_penalty_is_low_when_previous_strategy_is_likely ... ok
test_strategy_encoder_matches_paper_mlp_shape ... ok
test_op4_outputs_are_bounded_and_finite_across_team_sizes ... ok
test_op4_samples_multiple_behavior_regimes ... ok
test_op4_sampling_is_reproducible_with_fixed_seed ... ok

Ran 6 tests in 1.835s

OK
```

Pass/fail summary:

- `pytest`: not runnable, runner missing
- `unittest`: 6 passed, 0 failed, 0 errors

Additional schema smoke helper:

```text
[test-vec-schema] GPU core get_obs() vec: dtype=float32, shape=(B,N,18), finite, in bounds. OK.
```

## 5. Baseline Smoke Run

I used the project training function directly so the rollout could be reduced to 8 timesteps.

Command:

```powershell
$env:PYTHONPATH='...\AICTFProject\.venv\Lib\site-packages'
& '...\codex-primary-runtime\dependencies\python\python.exe' -c "from rl.train_ppo import PPOConfig, train_ppo; cfg=PPOConfig(); cfg.use_latent_strategy=False; cfg.total_timesteps=8; cfg.n_envs=1; cfg.n_steps=8; cfg.batch_size=8; cfg.n_epochs=1; cfg.device='cpu'; cfg.enable_progress_bar=False; cfg.enable_tensorboard=False; cfg.enable_checkpoints=False; cfg.enable_eval=False; cfg.checkpoint_dir='checkpoints_sb3/2v2'; cfg.run_tag='codex_phase0_smoke_2v2'; train_ppo(cfg)"
```

Wall time: about 6 seconds.

Result: success. Important stdout:

```text
[PPO] Agents: 2 per team (2v2) | mode=FIXED_OPPONENT | run_tag='codex_phase0_smoke_2v2'
[PPO] Total timesteps: 8
[PPO] Using GPU-native batched env: n_envs=1, agents=2v2, device=cpu
[PPO] Using stable MARL PPO: lr=1.5e-4, n_epochs=2, clip_range=0.10, ent_coef=0.005, batch_size=1024, target_kl=0.02
[PPO] Adjusting batch_size for rollout size: 1024->8 (n_steps*n_envs=8)
[PPO] Training complete. Final model saved to: checkpoints_sb3/2v2\final_codex_phase0_smoke_2v2.zip
PPO: 100%|##########| 8.00/8.00 [00:00<00:00, 73.6 step/s]
```

Generated artifact:

- `AICTFProject/checkpoints_sb3/2v2/final_codex_phase0_smoke_2v2.zip`

## 6. Architecture Census

Search terms: `class .*nn.Module`, `class .*torch.nn.Module`, `nn.Module`, `Conv2d`, `Linear`, `LSTM`, `GRU`, `Transformer`, `MultiheadAttention`, `attention`.

No `nn.LSTM`, `nn.GRU`, `nn.Transformer*`, `nn.MultiheadAttention`, or custom attention classes were found in tracked Python source.

| Module | Location | Input Shape(s) | Layer Types | Instantiated / Used |
| --- | --- | --- | --- | --- |
| `_PerAgentGridCNN` | `AICTFProject/rl/ctf_cnn_extractor.py` | `(B, C, H, W)` | `Conv2d`, `ReLU`, `MaxPool2d`, `AdaptiveAvgPool2d`, `Flatten`, `Linear` | `TokenizedCombinedExtractor`; `LatentMaskedMultiInputPolicy.local_grid_encoder` |
| `TokenizedCombinedExtractor` | `AICTFProject/rl/ctf_cnn_extractor.py` | dict: `grid (B, M, C, H, W)`, `vec (B, M, V)`, optional `context` | per-agent CNN plus vector flatten/concat | Vanilla PPO `policy_kwargs` in `train_ppo.py`; also forced into latent policy superclass |
| `StrategyEncoder` | `AICTFProject/rl/latent_marl.py` | `(B, GLOBAL_STATE_DIM)` where `GLOBAL_STATE_DIM == 32` | `Linear`, `ReLU`, `Linear`, `ReLU`, `Linear` | `LatentMaskedMultiInputPolicy.strategy_encoder`; attached into `LatentStrategyVecEnvWrapper` |
| `LatentMaskedMultiInputPolicy` | `AICTFProject/rl/latent_marl.py` | obs dict with `grid`, `vec`, `mask`, `z_idx`, `z_onehot`, `global_state`; actions `MultiDiscrete` | CNN local-grid encoder, MLP actor, linear action head, MLP action-conditioned critic | Selected in `train_ppo.py` when `use_latent_strategy=True` |
| `LatentStrategyPPO` | `AICTFProject/rl/latent_marl.py` | SB3 rollout minibatches | PPO losses plus strategy entropy and persistence terms | Selected in `train_ppo.py` when `use_latent_strategy=True` |
| `MaskedMultiInputPolicy` | `AICTFProject/rl/train_ppo.py` | obs dict from SB3 vec env | SB3 feature extractor, SB3 `mlp_extractor`, linear action/value nets, action-mask logit fill | Vanilla baseline policy selected when `use_latent_strategy=False` |

Important architecture findings for later phases:

- The local grid observation already goes through convolutional layers before flattening in both vanilla and latent paths.
- The vanilla path still uses SB3's `mlp_extractor` and linear action/value heads after the feature extractor.
- The latent path is already implemented and is currently the default, which conflicts with the audit plan's stated scope boundary of preparing for ICRA without implementing latent strategy yet.
- The centralized/global state is currently padded to 32 floats with 18 meaningful fields, not the audit plan's required 14-float schema.
- The project still has many `cnn` names that refer to checkpoint/run naming, not necessarily concrete CNN modules.
- The active environment API is custom Gymnasium/SB3 `VecEnv`, not PettingZoo.

## Phase 0 Acceptance Status

- Repo inventory: complete.
- Dependency inventory: complete; no tracked manifest exists.
- Entry points: complete.
- Existing tests: attempted `pytest`; captured missing-runner failure; ran `unittest` fallback successfully.
- Baseline smoke run: complete and successful.
- Architecture census: complete.
- Source modifications: no source files were edited during Phase 0. The only audit edit is this report file. The pre-existing modified `AICTFProject/plot/plot_metrics.py` remains untouched.

## Phase 0 Progress Note

Changed: added this baseline audit report only.

Verified: dependency state, CLI surfaces, current test runner situation, unittest pass, vec schema helper, and an 8-step vanilla PPO smoke run.

Open risks for Phase 1+: no dependency manifest; broken local venv launcher; default training path already uses latent strategy; global state shape/schema does not match the requested 14-float CTDE contract; pytest is missing; there is a pre-existing modified plotting source file and untracked checkpoint directory.
